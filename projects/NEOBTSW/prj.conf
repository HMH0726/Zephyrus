# minimal test config
CONFIG_GPIO=y
CONFIG_MAIN_STACK_SIZE=2048
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=3

# Bluetooth base (keep minimal for now)
CONFIG_BT=y
CONFIG_BT_PERIPHERAL=y

# settings only if you need persistence; 可以先註解看能否過
CONFIG_SETTINGS=y