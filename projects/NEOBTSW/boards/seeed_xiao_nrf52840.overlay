/* Overlay: 使用板上的 sw0（如果沒實體按鈕，你需要接外部按鈕到相對應腳位）
   這個 overlay 只是範例，將 sw0 綁到 P0.11 (修改為你實際使用之腳位)
*/
&gpio0 {
    compatible = "vnd,button-sw0";
    sw0: sw0 {
        gpios = <0 11 0>; /* port 0 pin 11, active low (modify as needed) */
        label = "sw0";
        debounce-delay-ms = <50>;
    };
};